apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: devportal-base
  description: VeeCode DevPortal Base Image
  # Example for optional annotations
  annotations:
    github.com/project-slug: veecode-platform/devportal-base
    github.com/workflows: |
     [
       {
         "workflow": "echo.yml",
         "label": "Echo",
         "tooltip": "Click here and start an useless workflow"
       }
     ]
  #   backstage.io/techdocs-ref: dir:.
spec:
  type: website
  owner: group:developers
  lifecycle: production
  system: devportal
